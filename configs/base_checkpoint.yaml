# Frozen SR-CorrNet var-2-5 checkpoint (BLUEPRINT §3.5, task P0-B1)
# Run scripts/download_checkpoint.py to populate sha256 after first download.

checkpoint_id: shinuh/sr-corrnet-ss-1ch-wsj-var-2-5spk
local_path: sr_corrnet/checkpoints/SS/1ch_WSJ_var_2_5spk/model.pt
sha256: ""  # fill after: python scripts/download_checkpoint.py

# Architecture constants, locked by checkpoint weights, never change
sample_rate: 8000
max_n_spks: 5
n_enc: 2
n_dec: 4
d_model: 128
stft_window: 128
stft_hop: 64
freq_bins: 65

# Configurable inference parameter (exposed via prob_thres patch, BLUEPRINT §15.4)
prob_thres: 0.5

# Published baseline on clean WSJ0-mix (BLUEPRINT §3.3)
baseline_performance:
  count_accuracy_pct:
    n2: 100.0
    n3: 99.7
    n4: 97.7
    n5: 96.9
  si_sdri_db:
    n2: 24.8
    n3: 24.4
    n4: 21.9
    n5: 19.9
